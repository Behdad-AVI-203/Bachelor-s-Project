[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: tests that exercise multiple platform layers
    performance: virtual-clock scale and runtime tests
    ui: Streamlit AppTest integration tests
